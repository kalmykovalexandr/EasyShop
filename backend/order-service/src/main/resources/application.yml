server:
  port: 9003
spring:
  application:
    name: order-service
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: ${SPRING_JPA_PROPERTIES_HIBERNATE_DEFAULT_SCHEMA:orders}
jwt:
  secret: ${JWT_SECRET}
logging:
  pattern:
    level: "%5p [trace:%X{traceId}]"
